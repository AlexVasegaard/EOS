import docplex.mp.model as cpx
import numpy as np
import pandas as pd
from cvxopt import matrix


def solve(score, **kwargs):

    raise Exception("CPLEX Solvers is currently broken, due to dependecy conflicts with older versions of Python and Numpy. Will hopefully be resolved soon!")

    return ...
